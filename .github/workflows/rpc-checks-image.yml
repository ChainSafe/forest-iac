name: rpc-checks image

on:
  push:
    branches:
      - main
    paths:
      - 'scripts/rpc-checks/**'
      - '.github/workflows/rpc-checks-image.yml'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-slim
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # ubuntu-slim ships the docker client, compose and buildx, but no engine,
      # and every local buildx driver needs a daemon to build in.
      - name: Set up Docker
        uses: docker/setup-docker-action@v5

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: scripts/rpc-checks
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: |
            ghcr.io/chainsafe/forest-rpc-checks:latest
            ghcr.io/chainsafe/forest-rpc-checks:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
